from django.apps import AppConfig


class SimulatedPersonasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.simulated_personas"
    verbose_name = "Simulated Personas"
